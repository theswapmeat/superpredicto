# === Python ===
__pycache__/
*.py[cod]
server/__pycache__/
server/**/*.py[cod]

# Virtual environment
server/venv/

# Flask / Django / other server frameworks
*.log
*.sqlite3
instance/
.env*

# === Node / Frontend (client/) ===
client/node_modules/
client/dist/
client/.next/
client/.env.local
client/.env.*.local
client/.turbo/
client/yarn.lock
client/package-lock.json

# === VS Code ===
.vscode/

# === Database backups (contain full DB dump — never commit) ===
backup/
*.backup
*.backup.gz
*.dump
*.sql.gz

# === DigitalOcean Functions deploy artifacts (regenerated by `doctl serverless deploy`) ===
infra/do-functions/**/.deployed/
infra/do-functions/**/__deployer__.zip

# === System Files ===
.DS_Store
Thumbs.db

# === Local dev/test seed + preview scripts (not for the repo) ===
server/seed_dummy_dev.py
server/_render_demo.py

# === Random Files ===
fifa_club_world_cup_2025_schedule.json
schedule.json
supabase_schema.sql
createtables.py
test_connection.py